version: '3.8'

services:
  resumeiq:
    build: .
    ports:
      - "5000:5000"
    environment:
      - APP_ENV=development
      - STORAGE_MODE=local
      - DATABASE_MODE=sqlite
    volumes:
      - ./storage:/app/storage
      - ./data:/app/data
